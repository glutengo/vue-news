overwrite: true
schema: 'server/schema.gql'
generates:
  src/main/webapp/app/graphql.ts:
    plugins:
      - 'typescript'
      - 'graphql-typeop/plugins'
    config:
      skipTypename: true
      maybeValue: T | undefined
      withHooks: false
